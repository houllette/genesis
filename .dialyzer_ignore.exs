# Warnings to skip, as {file, warning_type} tuples. Empty on purpose: the
# stock generated app produces none. Add an entry only for a warning inside a
# dependency that isn't ours to fix, and say which one and why.
[]
